import { Skeleton } from "@/components/ui/skeleton";

const UserDetailsLoader = () => {
  return <Skeleton className="rounded-lg w-full h-full aspect-square" />;
};

export default UserDetailsLoader;
